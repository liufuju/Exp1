1. Object Participant for passing data between apps
	1.1 self.participants.var[key] = value
2. the apps created for different stage of the experiment
3. constant is the parameter that is constant to each parcitipant, thus it is not suitable for condition setting 
4. transfering data to templates ~ def vars_for_template(), return a dict, and refer to the key by {{  }}
5. finish the basic structure first, refine it later (description, css, js, etc.) 
6. js设置内容的出现与否 ～ display: none就可以，直接document来call它就好
7. radio的大小可以直接用height width处理


Attention:
1. emotion instructions remains undetermined. 

